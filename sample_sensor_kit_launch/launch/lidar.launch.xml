<launch>
  <arg name="launch_driver" default="true"/>
  <arg name="host_ip" default="192.168.1.10"/>
  <arg name="use_concat_filter" default="true"/>
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)"/>
  <arg name="vehicle_mirror_param_file"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>

  <group>
    <push-ros-namespace namespace="lidar"/>

    <group>
      <push-ros-namespace namespace="top"/>

      <!-- Livox Mid-360 Driver (Only runs if launch_driver is true) -->
      <group if="$(var launch_driver)">
        <include file="$(find-pkg-share livox_ros_driver2)/launch_ROS2/msg_MID360.launch.py">
          <arg name="user_config_path" value="$(find-pkg-share go2_sensor_kit_launch)/config/MID360_config.json"/>
          <arg name="frame_id" value="lidar_top"/>
        </include>
      </group>

      <!-- Livox Pointcloud Preprocessor -->
      <node pkg="livox_laser_simulation" exec="livox_laser_simulation_node" name="livox_preprocessor" output="screen">
        <remap from="input" to="/livox/lidar"/>
        <remap from="output" to="pointcloud_raw"/>
      </node>

      <!-- Autoware Pointcloud Preprocessor -->
      <include file="$(find-pkg-share go2_sensor_kit_launch)/launch/pointcloud_preprocessor.launch.py">
        <arg name="base_frame" value="base_link"/>
        <arg name="use_intra_process" value="true"/>
        <arg name="use_multithread" value="true"/>
        <arg name="pointcloud_container_name" value="$(var pointcloud_container_name)"/>
      </include>

    </group>
  </group>
</launch>
